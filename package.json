{
  "name": "tech-interview-starter",
  "version": "1.0.0",
  "description": "Technical interview starter project with Vue and Express",
  "private": true,
  "scripts": {
    "dev": "concurrently \"pnpm run dev:server\" \"pnpm run dev:client\"",
    "dev:client": "cd client && pnpm run dev",
    "dev:server": "cd server && pnpm run dev",
    "build": "pnpm run build:client && pnpm run build:server",
    "build:client": "cd client && pnpm run build",
    "build:server": "cd server && pnpm run build",
    "lint": "pnpm run lint:client && pnpm run lint:server",
    "lint:client": "cd client && pnpm run lint",
    "lint:server": "cd server && pnpm run lint"
  },
  "keywords": ["interview", "vue", "express", "typescript"],
  "author": "",
  "license": "MIT",
  "devDependencies": {
    "concurrently": "^8.2.2",
    "typescript": "^5.4.2"
  }
}
